// settings-test.gradle
// Dùng để chạy test mà KHÔNG cần Android plugin (yêu cầu Java 11+)
// Chạy test bằng lệnh:
//   .\gradlew.bat :core:test --settings-file settings-test.gradle
include 'core', 'lwjgl3'
